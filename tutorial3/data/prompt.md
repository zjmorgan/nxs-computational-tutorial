- NotebookLM

    ```
    How can advanced AI models, particularly flow-based methods, enhance the prediction and design of novel magnetoelectric materials?
    ```

- generating files

    > Try with `Warp` first

    ```
    generate 10 files with some dummy data in it. keep it two columns. x axis should be sequential numbers and y axis should be some random number. each file should contain 100 rows.
    ```

    > Try again with Gemini CLI

- fitting the data

    ```
    give me a Python script. I want to read in all data files in my current directory. Data files should start with 'data' in the file name. for each of the file, I want to perform a linear fitting. then generate a plot to show both the data and the fitting, together with the R-factor shown on the plot. do not show the plot interactively, just save it to disk.
    ```

- do something with files

    ```
    I want to rename files ending with .csv. only rename those files with odd number in the file name. replace the odd number with 'odd_blabla_NUM' where 'NUM' refers to a number equal to the original odd number plus 10.
    ```

- VSCode demo

    > first, in the chat box

    ```
    make the font bigger for the axis label and tick marks
    ```

    > next inside the script

    ```
    # Here we want to change the legend location to upper right
    ```

- dify demo

    ```
    what is neutron total scattering and how is that different from bragg diffraction
    ```

    ```
    send the info to slack
    ```

    ```
    send the current weather condition in knoxville TN to slack
    ```
